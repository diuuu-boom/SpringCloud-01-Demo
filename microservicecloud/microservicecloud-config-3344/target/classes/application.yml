server:
  port: 3344
  
spring: 
  application:
    name: microservicecloud-config
  cloud: 
    config: 
      server:
        git: 
          uri: git@github.com:qovd520/microservicecloud-config.git    # git 仓库的 ssh